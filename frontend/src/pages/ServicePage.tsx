import {Link,useParams} from 'react-router-dom';import {services} from '../content/services';import {NotFoundPage} from './NotFoundPage';
export function ServicePage(){const {slug}=useParams();const s=services.find(s=>s.slug===slug);if(!s)return <NotFoundPage/>;return <><section className="service-detail wrap section"><div><Link className="eyebrow text-link" to="/tjanster">← Alla tjänster</Link><h1>{s.title}<span className="place">i Trollhättan.</span></h1><p className="lead">{s.intro}</p><p>{s.description}</p><div className="price-tag"><strong>{s.confirmed?'Cirka ':''}{s.price} kr</strong><span>{s.duration} · {s.confirmed?'ungefärligt pris':'demopris'}</span></div><Link className="button" to={'/boka?tjanst='+s.slug}>Välj {s.title.toLowerCase()} ↗</Link><p className="note">DEMO · Behandlingsinnehåll och tid är förslag. Slutligt pris bekräftas av salongen.</p></div><img src={'/images/'+s.image} alt={'Inspirationsbild för '+s.title} /></section><section className="wrap section compact"><h2>Detaljerna i besöket.</h2><div className="steps">{s.details.map((d,i)=><article key={d}><span>0{i+1}</span><h3>{d}</h3></article>)}</div><Link className="text-link" to="/prislista">Jämför alla priser ↗</Link></section></>}
