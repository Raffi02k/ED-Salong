import {readFile,writeFile,mkdir} from 'node:fs/promises';import {dirname,resolve} from 'node:path';import {render,renderHead,routePaths,site} from '../.ssr/entry-server.js';const template=await readFile('dist/index.html','utf8');for(const route of [...routePaths,'/404']){const html=template.replace(/<!--app-head-->[\s\S]*?<!--\/app-head-->/,renderHead(route)).replace('<!--app-html-->',render(route));const path=resolve('dist',route==='/'?'index.html':route.slice(1)+'/index.html');await mkdir(dirname(path),{recursive:true});await writeFile(path,html);if(route==='/404')await writeFile('dist/404.html',html)}await writeFile('dist/robots.txt',site.indexable?`User-agent: *\nAllow: /\nSitemap: ${site.url}/sitemap.xml\n`:'User-agent: *\nDisallow: /\n');await writeFile('dist/sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${routePaths.filter(p=>!['/boka','/integritet'].includes(p)).map(p=>`<url><loc>${site.url}${p}</loc></url>`).join('')}</urlset>`);console.log('Prerendered '+(routePaths.length+1)+' pages');